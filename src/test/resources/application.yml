spring:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa

env:
  base-url: https://www.test.kr